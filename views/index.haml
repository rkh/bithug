%h1 Repository index
.login
  %form{:method => "post", :action => "login"}
    Username:
    %input{:name => "name", :type => "text"}
    Password:
    %input{:name => "password", :type => "text"}
    %input{:value => "Submit", :type => "submit"}
- if !user.nil?
  #user
    %h2= user.name
    - user.repositories.each do |r|
      %a{:href => "/#{user.name}/#{r.name}"}
        = r.name
