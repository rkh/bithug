.infoBox
  %h1= repo.name
  %pre git clone #{repo.git_url}
  - if owned?
    %a{:href => "/#{user.name}/#{repo.name}/admin"} admin
  
  - else
    %a{:href => "/#{user.name}/#{repo.name}/fork"} fork

.files
  %pre= tree.inspect
